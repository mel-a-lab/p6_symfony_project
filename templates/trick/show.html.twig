{% extends 'base.html.twig' %}

{% block title %}Trick{% endblock %}

{% block body %}
    <h1>Figure</h1>

    <table class="table">
        <tbody>
        <div style="position: relative;">
    <img src="{{ asset('homepage_image/pexels-evgenia-kirpichnikova-1973293.jpg') }}" alt="image d'accueil" class="img-fluid w-100">
    <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);">
            <tr>
                <th>Id</th>
                <td>{{ trick.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trick.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_trick_index') }}">back to list</a>

    <a href="{{ path('app_trick_edit', {'slug': trick.slug}) }}">edit</a>

    {{ include('trick/_delete_form.html.twig') }}
{% endblock %}
