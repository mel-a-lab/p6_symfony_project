<form method="post" action="{{ path('app_trick_image_delete', {'id': trick_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick_image.id) }}">
    <button class="btn">Delete</button>
</form>
