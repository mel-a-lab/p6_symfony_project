<form method="post" action="{{ path('app_trick_video_delete', {'id': trick_video.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete-trick-video') }}">
	<button class="btn">Supprimer</button>
</form>

